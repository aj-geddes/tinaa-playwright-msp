apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tinaa-playwright-msp.fullname" . }}-test-connection"
  labels:
    {{- include "tinaa-playwright-msp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tinaa-playwright-msp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
