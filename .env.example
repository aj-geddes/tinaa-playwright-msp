# Sample environment variables for Tina Tester MCP
# Copy this file to .env and customize as needed

# Server settings
PORT=8000
HOST=0.0.0.0
DEBUG=1

# Playwright settings
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_SLOW_MO=0

# Credentials (optional)
# These are used only if user doesn't provide credentials interactively
# Format: CREDENTIALS_DOMAIN_USERNAME=username
# Format: CREDENTIALS_DOMAIN_PASSWORD=password
# Example:
# CREDENTIALS_EXAMPLE_COM_USERNAME=test_user
# CREDENTIALS_EXAMPLE_COM_PASSWORD=test_password
